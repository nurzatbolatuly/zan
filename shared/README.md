# @zan/shared

Общий код, переиспользуемый между `backend` и `ingestion`: типы контрактов агентов
(вход/выход каждого агента), структурированный логгер, общие утилиты валидации.

Наполняется начиная с Этапа 2 (см. `../PLAN.md`). Пока — пустой пакет-заготовка.
